version: '3.9'

services:
  db:
    image: postgres:14.5-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_USER=dpm
      - POSTGRES_PASSWORD=dpm
    volumes:
      - /var/lib/postgresql/data
    ports:
      - "5434:5432"
